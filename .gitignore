# Dépendances (le dossier le plus lourd, à ignorer absolument)
node_modules/

# Environnement (contient tes mots de passe de BDD, ne jamais pusher !)
.env

# Fichiers système Mac
.DS_Store

# Dossiers Docker (si tu as des volumes locaux)
.docker/

# Build / Dist (si tu en as)
dist/
build/

# Logs
npm-debug.log*
# Fichiers système macOS (tous niveaux)
**/.DS_Store

# Configuration locale de l'agent (non pertinente pour le jury)
.claude/

# Notes de travail et documents de soutenance (hors code source)
SUIVI-PROJET.md
*.docx
~$*
